model:
  num_encoders: 2
  num_decoders: 2

  d_model: 256
  num_heads: 8
  dropout: 0.1

training:
  epochs: 30
  save_path: 'models'
  load_path: ''

  num_frames: 30
  num_workers: 0
  seed: 29
  split: [0.8, 0.1, 0.1]
  batch_size: 4
  lr: 1e-4

testing:
  load_path: 'models/best.pt'
  beam_search: false